info:
  name: Test pay
  type: http
  seq: 7

http:
  method: POST
  url: " http://localhost:8000/pay"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
