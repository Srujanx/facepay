info:
  name: Register User
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:8000/auth/register
  body:
    type: json
    data: |-
      {
        "email": "test@test.com",
        "password": "testpass123",
        "full_name": "Test User",
        "fare_category": "adult"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
