info:
  name: Test Embed
  type: http
  seq: 4

http:
  method: POST
  url: http://localhost:8000/embed
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
