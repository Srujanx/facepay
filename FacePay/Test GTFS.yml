info:
  name: Test GTFS
  type: http
  seq: 8

http:
  method: GET
  url: http://localhost:8000/gtfs/route-status?route_id=110
  params:
    - name: route_id
      value: "110"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
